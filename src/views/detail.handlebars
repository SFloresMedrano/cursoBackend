<h1>{{product.title}} Detail</h1>
<h3>Id: {{product.id}}</h3>
    <div>
        <p>Id:{{product._id}}</p>
        <p>Description:{{tproduct.escription}}</p>
        <p>Price: {{product.price}}</p>
        <p>Code: {{product.code}}</p>
        <p>Stock: {{product.stock}}</p>
        <p>Category: {{product.category}}</p>

        <button onclick="putIntoCart('{{product._id}}')">Add to cart {{product._id}}</button>
    </div>